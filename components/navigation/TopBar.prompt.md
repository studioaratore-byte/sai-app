**TopBar** — the big bold title that opens a screen (TDS "Top"). One clear sentence; the description gives the one-line "why". Its title should match the `NavigationBar` title that fades in on scroll.

```jsx
<TopBar title={"언제 만날까요?"} description="싫은 시간만 지우면, 사이가 좋은 시간을 찾아드려요." />
```
