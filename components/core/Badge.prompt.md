**Badge** — compact status or count label. Soft-tinted by default (`weak`), or solid fill for emphasis.

```jsx
<Badge tone="primary">추천</Badge>
<Badge tone="negative" weak={false}>마감</Badge>
<Badge tone="neutral">선택</Badge>
```

Tones: `neutral` · `primary` · `positive` · `negative` · `warning`.
